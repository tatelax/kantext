* Need to fix checking who is modifying what task
* Change number of tasks passing or failing to use a filled circle to show what percent of tasks passed or failed as a visual indicator
* Make logo
* Rename "TODO" to "Inbox"
* Make Inbox/todo, in progress, and done impossible to remove. If they dont exist, the web server should add them
* Add a pencil icon to rename custom columns but the default ones should not be able to be deleted
* Make task view wider
* Lower opacity on blockers behind task view and dialog boxes
* Add indicator to stale tasks. Define what stale means in config.json
* Add config menu to modify config.json from UI
* Add strikethru on tasks in done column
* If a task fails to pass ask the user if they want to move it to in progress