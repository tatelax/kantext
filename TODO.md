X Update the task view so that it doesnt have a blocking background at all when it's open. Instead put a blue outline around the task card that is selected.
X Fix the header layout of the Task View so that the start of the task ID is aligned to the start of the task title
* Move the metadata of the Task View to be part of the header.
* Update 